* From 0.0.2
	- add generator csp_report:install to retrieve the migration files and add
	the gem routes

* From 0.0.1
	- Using haml rather than haml-rails to render the gem views without changing
	the app details
